# Sorryios AI 智能笔记系统 - 前端

> React + Vite + TailwindCSS 构建的现代化前端界面

---

## 🚀 快速开始

### 1. 确保后端正在运行

```powershell
cd D:\sorryios-test\backend
npm run dev
```

### 2. 安装前端依赖

```powershell
cd D:\sorryios-test\frontend
npm install
```

### 3. 启动前端开发服务器

```powershell
npm run dev
```

### 4. 打开浏览器

访问：`http://localhost:5173`

---

## 📁 项目结构

```
frontend/
├── index.html              # HTML入口
├── package.json            # 依赖配置
├── vite.config.js          # Vite配置（含代理）
├── tailwind.config.js      # TailwindCSS配置
├── postcss.config.js       # PostCSS配置
├── public/
│   └── favicon.svg         # 网站图标
└── src/
    ├── main.jsx            # React入口
    ├── App.jsx             # 主应用组件
    ├── index.css           # 全局样式
    ├── components/
    │   ├── FileUploader.jsx    # 文件上传组件
    │   ├── ProgressTracker.jsx # 进度跟踪组件
    │   └── ReportViewer.jsx    # 报告查看组件
    └── hooks/
        └── useTaskProgress.js  # WebSocket进度Hook
```

---

## ✨ 功能特性

- 📤 **拖拽上传** - 支持拖拽或点击选择文件
- 📊 **实时进度** - WebSocket 实时更新处理进度
- 📄 **报告预览** - 在线预览 HTML 报告
- ⬇️ **多格式下载** - HTML / Markdown / JSON

---

## 🔧 配置说明

### 代理配置（vite.config.js）

前端开发服务器会自动将 API 请求代理到后端：

- `/api/*` → `http://localhost:3000`
- `/outputs/*` → `http://localhost:3000`
- `/socket.io` → `http://localhost:3000`

### 端口说明

- 前端：`http://localhost:5173`
- 后端：`http://localhost:3000`

---

## 📱 界面预览

### 上传页面
- 拖拽上传区域
- 文件信息显示
- 开始处理按钮

### 处理页面
- 实时进度条
- 片段处理指示器
- 当前状态显示
- 取消按钮

### 报告页面
- 统计信息卡片
- 在线预览按钮
- 多格式下载

---

## 🛠️ 开发命令

```powershell
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

---

*文档更新：2026-01-11*
